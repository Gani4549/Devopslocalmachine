trigger:
- master

pool:
  name: Default

stages:

# =====================================================
# BUILD STAGE
# =====================================================

- stage: Build
  displayName: Build WAR Project

  jobs:

  - job: BuildWebApp
    displayName: Build WebApp WAR

    steps:

    # ==========================================
    # CHECKOUT SOURCE CODE
    # ==========================================

    - checkout: self

    # ==========================================
    # CHECK JAVA & MAVEN
    # ==========================================

    - script: |
        java --version
        mvn --version

      displayName: Check Java and Maven

    # ==========================================
    # BUILD WEBAPP MODULE
    # ==========================================

    - script: |
        mvn clean package -pl webapp -am

      displayName: Build WAR File

    # ==========================================
    # FIND GENERATED WAR FILE
    # ==========================================

    - script: |
        echo =====================================
        echo SEARCHING FOR WAR FILE
        echo =====================================

        dir /s *.war

      displayName: Find WAR File


# =====================================================
# DEPLOY STAGE
# =====================================================

- stage: Deploy
  displayName: Deploy WAR To Tomcat

  dependsOn: Build

  jobs:

  - job: DeployToTomcat
    displayName: Deploy WAR To Local Tomcat

    steps:

    # ==========================================
    # VERIFY TOMCAT WEBAPPS FOLDER
    # ==========================================

    - script: |
        dir "C:\Users\ABKO472\Downloads\apache-tomcat-11.0.21-windows-x64\apache-tomcat-11.0.21\webapps"

      displayName: Verify Tomcat Folder

    # ==========================================
    # COPY WAR TO TOMCAT
    # ==========================================

    - script: |
        for /r "$(Pipeline.Workspace)" %%i in (*.war) do copy "%%i" "C:\Users\ABKO472\Downloads\apache-tomcat-11.0.21-windows-x64\apache-tomcat-11.0.21\webapps"

      displayName: Copy WAR To Tomcat

    # ==========================================
    # RESTART TOMCAT
    # ==========================================

    - script: |
        call "C:\Users\ABKO472\Downloads\apache-tomcat-11.0.21-windows-x64\apache-tomcat-11.0.21\bin\shutdown.bat"

        timeout /t 10

        call "C:\Users\ABKO472\Downloads\apache-tomcat-11.0.21-windows-x64\apache-tomcat-11.0.21\bin\startup.bat"

      displayName: Restart Tomcat

    # ==========================================
    # VERIFY DEPLOYMENT
    # ==========================================

    - script: |
        dir "C:\Users\ABKO472\Downloads\apache-tomcat-11.0.21-windows-x64\apache-tomcat-11.0.21\webapps"

      displayName: Verify WAR Deployment